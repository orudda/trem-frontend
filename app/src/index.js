import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // ou o nome do seu componente principal
import "./index.css"; // ou o nome do seu arquivo de estilos

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
